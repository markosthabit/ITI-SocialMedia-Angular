import { FormatLikesPipe } from './format-likes-pipe';

describe('FormatLikesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLikesPipe();
    expect(pipe).toBeTruthy();
  });
});
